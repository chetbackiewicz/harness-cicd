apiVersion: v1
kind: Service
metadata:
  name: vuln-api
  namespace: vuln-api-dev
  labels:
    app: vuln-api
    env: dev
spec:
  type: ClusterIP
  selector:
    app: vuln-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
