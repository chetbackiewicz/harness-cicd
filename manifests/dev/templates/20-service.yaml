apiVersion: v1
kind: Service
metadata:
  name: vuln-api
  labels:
    app: vuln-api
spec:
  type: ClusterIP
  selector:
    app: vuln-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
